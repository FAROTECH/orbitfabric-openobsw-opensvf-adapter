[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "orbitfabric-openobsw-opensvf-adapter"
version = "0.1.0.dev0"
description = "OrbitFabric adapter for OpenOBSW/OpenSVF integration and verification projection."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [
  { name = "OrbitFabric Contributors" }
]
dependencies = [
  "PyYAML>=6.0",
  "jsonschema>=4.23",
  "rfc8785>=0.1.4"
]

[project.optional-dependencies]
dev = [
  "build>=1.2",
  "mkdocs-material>=9.5",
  "pytest>=8.0",
  "ruff>=0.5"
]

[project.scripts]
orbitfabric-openobsw-opensvf = "orbitfabric_openobsw_opensvf_adapter.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/orbitfabric_openobsw_opensvf_adapter"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = []
