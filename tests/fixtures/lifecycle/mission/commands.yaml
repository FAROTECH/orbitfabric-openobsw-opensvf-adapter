commands:
  - id: obc.ping
    target: obc
    description: Minimal lifecycle command used to validate the OpenOBSW/OpenSVF command path.
    arguments: []
    allowed_modes:
      - NOMINAL
      - SAFE
    requires_ack: true
    timeout_ms: 500
    risk: low
    emits:
      - obc.ping_requested
    expected_effects:
      no_state_change: true
